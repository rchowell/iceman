[project]
name = "scripts"
version = "0.1.0"
description = "Iceberg test table generator for Iceman"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyiceberg[pyiceberg-core,s3fs,sql,sql-sqlite]>=0.9.0",
    "pyarrow>=18.0.0",
    "numpy>=2.0.0",
    "pyyaml>=6.0",
]
